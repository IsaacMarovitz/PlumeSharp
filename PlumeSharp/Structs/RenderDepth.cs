namespace Plume
{
    public struct RenderDepth
    {
        public float Depth = 1.0f;

        public RenderDepth() { }

        public RenderDepth(float depth)
        {
            Depth = depth;
        }
    }
}
